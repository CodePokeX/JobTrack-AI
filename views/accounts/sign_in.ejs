<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign In</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/daisyui@2.51.6/dist/full.css" rel="stylesheet" type="text/css" />
</head>

<body class="font-roboto bg-gradient-to-r from-indigo-200 via-blue-100 to-teal-100 flex justify-center items-center min-h-screen text-black">

    <div class="flex flex-col items-center">
        <div class="flex items-center mb-8">  
            <img src="your_logo.png" alt="Company Logo" class="h-16 w-auto mr-4">  
            <h1 class="text-3xl font-bold text-black">Your Company Name</h1>  
        </div>
        <div class="card w-96 bg-white shadow-2xl rounded-lg p-6">
            <div class="card-body">
                <h2 class="text-3xl font-bold text-center mb-6 text-black">Sign In</h2>

                <form action="/sign-in" method="POST">
                    <div class="form-control w-full max-w-xs mb-4">
                        <label class="label">
                            <span class="label-text text-black">First Name</span>
                        </label>
                        <input type="text" name="firstname" class="input input-bordered w-full max-w-xs bg-neutral-100" required>
                    </div>
                    <div class="form-control w-full max-w-xs mb-4">
                        <label class="label">
                            <span class="label-text text-black">Last Name</span>
                        </label>
                        <input type="text" name="lastname" class="input input-bordered w-full max-w-xs bg-neutral-100" required>
                    </div>
                    <div class="form-control w-full max-w-xs mb-4">
                        <label class="label">
                            <span class="label-text text-black">Email</span>
                        </label>
                        <input type="email" name="email" class="input input-bordered w-full max-w-xs bg-neutral-100" required>
                    </div>
                    <div class="form-control w-full max-w-xs mb-6">
                        <label class="label">
                            <span class="label-text text-black">Password</span>
                        </label>
                        <input type="password" name="password" class="input input-bordered w-full max-w-xs bg-neutral-100" required>
                    </div>
                    <div class="form-control w-full max-w-xs mb-6">
                        <label class="label">
                            <span class="label-text text-black">Confirm Password</span>
                        </label>
                        <input type="password" name="passwordConfirmation" class="input input-bordered w-full max-w-xs bg-neutral-100" required>
                    </div>

                    <button type="submit" class="btn btn-primary w-full mt-4">Sign In</button>
                </form>

                <div class="mt-6 text-center">
                    <p class="text-sm text-black">By signing up, I agree to the <a href="#" class="text-blue-500 hover:underline">Terms of Service</a> and <a href="#" class="text-blue-500 hover:underline">Privacy Policy</a></p>
                </div>
            </div>
        </div>
    </div>

</body>

</html>
